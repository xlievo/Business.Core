未完成工作：
a：.json 思路
1：特性热更新需要对 Args(List) 属性直接赋值。(a：不能修改List集合。b：只能用foreach。才能线程安全)，启用=Enable，是否json特性=IsConfig

2：对比原则，不能删除原有特性，可删除添加json特性
   2.1：先拿原有List做对比
        2.1.1：先处理原有特性，不删除，设置启用/关闭，设置属性(对比原有List 如果属性值不一样)。不能删除。返回一个更改列表
		2.1.2：再处理json特性，添加启用，先筛选json启用的特性
			   1：对比原有List 如果没有就添加，有则属性，设置属性。返回一个添加列表
			   2：再处理json特性，删除筛选里面没有的。返回一个差异列表。

3：如果以上任何对比差异列表有值都将更新new 列表，最后排序code后 set Args(List)属性